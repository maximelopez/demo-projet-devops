apiVersion: v1
kind: Service
metadata:
  name: stats-service
  namespace: demo-devops
  labels:
    app: stats-service
spec:
  type: NodePort
  selector:
    app: stats-service
  ports:
  - protocol: TCP
    port: 81
    targetPort: 3000
    nodePort: 30081